# Encrypt/Decrypt Demo

This program is a demo for encryption and decryption. It uses an XOR cipher to implement simple symmetric key encryption. The program takes a message and key as input, and it outputs the encrypted message as a string of bytes, as well as decrypting it back to the original message.
